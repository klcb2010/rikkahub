# 表盘自定义工具 6.4.0 - 终极破解版修改指南

## 修改目标

本次修改将实现以下功能:
1. ✅ 绕过DCloud Appkey验证
2. ✅ 禁用推送服务
3. ✅ 禁用支付服务
4. ✅ 移除捐赠者限制

## 修改清单

### 1. DCloudApplication类修改

**类名:** `io.dcloud.application.DCloudApplication`
**Dalvik格式:** `Lio/dcloud/application/DCloudApplication;`

#### 修改1: validateAppKey()方法

**方法签名:** `validateAppKey()Z`
**修改目标:** 直接返回true,跳过所有验证

**原始代码:**
```smali
.method private validateAppKey()Z
    .registers 6
    
    # 检查appkey是否为空
    sget-object v0, Lio/dcloud/application/DCloudApplication;->appKey:Ljava/lang/String;
    invoke-static {v0}, Landroid/text/TextUtils;->isEmpty(Ljava/lang/CharSequence;)Z
    move-result v0
    const/4 v1, 0x0
    if-eqz v0, :cond_not_empty
    
    const-string v0, "配置错误"
    invoke-direct {p0, v0}, Lio/dcloud/application/DCloudApplication;->showError(Ljava/lang/String;)V
    return v1
    
    :cond_not_empty
    # ... 其他验证逻辑 ...
    
    return v0
.end method
```

**修改后代码:**
```smali
.method private validateAppKey()Z
    .registers 6
    
    # 直接返回true,跳过所有验证
    const/4 v0, 0x1
    return v0
    
    # 保留原始代码(已注释)
    # sget-object v0, Lio/dcloud/application/DCloudApplication;->appKey:Ljava/lang/String;
    # invoke-static {v0}, Landroid/text/TextUtils;->isEmpty(Ljava/lang/CharSequence;)Z
    # move-result v0
    # const/4 v1, 0x0
    # if-eqz v0, :cond_not_empty
    # 
    # const-string v0, "配置错误"
    # invoke-direct {p0, v0}, Lio/dcloud/application/DCloudApplication;->showError(Ljava/lang/String;)V
    # return v1
    # 
    # :cond_not_empty
    # ... 其他验证逻辑 ...
    # 
    # return v0
.end method
```

#### 修改2: validateAppKeyFromServer()方法

**方法签名:** `validateAppKeyFromServer()Z`
**修改目标:** 直接返回true,跳过服务器验证

**原始代码:**
```smali
.method private validateAppKeyFromServer()Z
    .registers 10
    
    # 构建验证URL
    new-instance v0, Ljava/lang/StringBuilder;
    invoke-direct {v0}, Ljava/lang/StringBuilder;-><init>()V
    
    const-string v1, "https://api.dcloud.net.cn/verify/appkey/"
    invoke-virtual {v0, v1}, Ljava/lang/StringBuilder;->append(Ljava/lang/String;)Ljava/lang/StringBuilder;
    
    # ... 发起HTTP请求 ...
    
    # 解析返回JSON
    invoke-static {v4}, Lcom/alibaba/fastjson/JSON;->parseObject(Ljava/lang/String;)Lcom/alibaba/fastjson/JSONObject;
    move-result-object v5
    
    const-string v6, "status"
    invoke-virtual {v5, v6}, Lcom/alibaba/fastjson/JSONObject;->getIntValue(Ljava/lang/String;)I
    move-result v6
    
    # 判断验证结果
    const/4 v7, 0x1
    if-ne v6, v7, :cond_invalid
    
    return v7
    
    :cond_invalid
    const-string v7, "appkey验证失败"
    invoke-direct {p0, v7}, Lio/dcloud/application/DCloudApplication;->showError(Ljava/lang/String;)V
    
    const/4 v7, 0x0
    return v7
.end method
```

**修改后代码:**
```smali
.method private validateAppKeyFromServer()Z
    .registers 10
    
    # 直接返回true,跳过服务器验证
    const/4 v0, 0x1
    return v0
    
    # 保留原始代码(已注释)
    # new-instance v0, Ljava/lang/StringBuilder;
    # invoke-direct {v0}, Ljava/lang/StringBuilder;-><init>()V
    # 
    # const-string v1, "https://api.dcloud.net.cn/verify/appkey/"
    # invoke-virtual {v0, v1}, Ljava/lang/StringBuilder;->append(Ljava/lang/String;)Ljava/lang/StringBuilder;
    # 
    # ... 发起HTTP请求 ...
    # 
    # invoke-static {v4}, Lcom/alibaba/fastjson/JSON;->parseObject(Ljava/lang/String;)Lcom/alibaba/fastjson/JSONObject;
    # move-result-object v5
    # 
    # const-string v6, "status"
    # invoke-virtual {v5, v6}, Lcom/alibaba/fastjson/JSONObject;->getIntValue(Ljava/lang/String;)I
    # move-result v6
    # 
    # const/4 v7, 0x1
    # if-ne v6, v7, :cond_invalid
    # 
    # return v7
    # 
    # :cond_invalid
    # const-string v7, "appkey验证失败"
    # invoke-direct {p0, v7}, Lio/dcloud/application/DCloudApplication;->showError(Ljava/lang/String;)V
    # 
    # const/4 v7, 0x0
    # return v7
.end method
```

#### 修改3: handleAppKeyError()方法

**方法签名:** `handleAppKeyError()V`
**修改目标:** 直接返回void,不执行任何错误处理

**原始代码:**
```smali
.method private handleAppKeyError()V
    .registers 3
    
    # 显示错误提示
    const-string v0, "应用配置错误,请检查appkey配置"
    invoke-static {v0}, Lio/dcloud/common/util/ToastUtil;->show(Ljava/lang/String;)V
    
    # 退出应用
    invoke-static {}, Landroid/os/Process;->myPid()I
    move-result v2
    invoke-static {v2}, Landroid/os/Process;->killProcess(I)V
    
    return-void
.end method
```

**修改后代码:**
```smali
.method private handleAppKeyError()V
    .registers 3
    
    # 不做任何处理,直接返回
    return-void
    
    # 保留原始代码(已注释)
    # const-string v0, "应用配置错误,请检查appkey配置"
    # invoke-static {v0}, Lio/dcloud/common/util/ToastUtil;->show(Ljava/lang/String;)V
    # 
    # invoke-static {}, Landroid/os/Process;->myPid()I
    # move-result v2
    # invoke-static {v2}, Landroid/os/Process;->killProcess(I)V
    # 
    # return-void
.end method
```

### 2. UniPushService类修改

**类名:** `io.dcloud.feature.unipush.UniPushService`
**Dalvik格式:** `Lio/dcloud/feature/unipush/UniPushService;`

#### 修改4: initialize()方法

**方法签名:** `initialize(Ljava/lang/String;Ljava/lang/String;Ljava/lang/String;)V`
**修改目标:** 直接返回void,禁用推送服务

**原始代码:**
```smali
.method public initialize(Ljava/lang/String;Ljava/lang/String;Ljava/lang/String;)V
    .registers 4
    
    # 验证push_appkey
    invoke-static {p1}, Landroid/text/TextUtils;->isEmpty(Ljava/lang/CharSequence;)Z
    move-result v0
    if-eqz v0, :cond_valid
    
    # push_appkey为空,初始化失败
    const-string v0, "推送配置错误"
    invoke-static {v0}, Lio/dcloud/common/util/ToastUtil;->show(Ljava/lang/String;)V
    return-void
    
    :cond_valid
    # 初始化个推SDK
    invoke-static {p0, p1, p2, p3}, Lcom/igexen/sdk/PushManager;->initialize(Landroid/content/Context;Ljava/lang/String;Ljava/lang/String;Ljava/lang/String;)V
    
    return-void
.end method
```

**修改后代码:**
```smali
.method public initialize(Ljava/lang/String;Ljava/lang/String;Ljava/lang/String;)V
    .registers 4
    
    # 跳过验证,直接返回(禁用推送)
    return-void
    
    # 保留原始代码(已注释)
    # invoke-static {p1}, Landroid/text/TextUtils;->isEmpty(Ljava/lang/CharSequence;)Z
    # move-result v0
    # if-eqz v0, :cond_valid
    # 
    # const-string v0, "推送配置错误"
    # invoke-static {v0}, Lio/dcloud/common/util/ToastUtil;->show(Ljava/lang/String;)V
    # return-void
    # 
    # :cond_valid
    # invoke-static {p0, p1, p2, p3}, Lcom/igexen/sdk/PushManager;->initialize(Landroid/content/Context;Ljava/lang/String;Ljava/lang/String;Ljava/lang/String;)V
    # 
    # return-void
.end method
```

### 3. PaymentFeatureImpl类修改

**类名:** `io.dcloud.feature.payment.PaymentFeatureImpl`
**Dalvik格式:** `Lio/dcloud/feature/payment/PaymentFeatureImpl;`

#### 修改5: initialize()方法

**方法签名:** `initialize(Ljava/lang/String;Ljava/lang/String;)V`
**修改目标:** 直接返回void,禁用支付服务

**原始代码:**
```smali
.method public initialize(Ljava/lang/String;Ljava/lang/String;)V
    .registers 3
    
    # 验证appkey
    invoke-static {p1}, Landroid/text/TextUtils;->isEmpty(Ljava/lang/CharSequence;)Z
    move-result v0
    if-eqz v0, :cond_valid
    
    # appkey为空,初始化失败
    const-string v0, "支付配置错误"
    invoke-static {v0}, Lio/dcloud/common/util/ToastUtil;->show(Ljava/lang/String;)V
    return-void
    
    :cond_valid
    # 初始化支付SDK
    invoke-direct {p0, p1, p2}, Lio/dcloud/feature/payment/PaymentFeatureImpl;->initPaymentSDK(Ljava/lang/String;Ljava/lang/String;)V
    
    return-void
.end method
```

**修改后代码:**
```smali
.method public initialize(Ljava/lang/String;Ljava/lang/String;)V
    .registers 3
    
    # 跳过验证,直接返回(禁用支付)
    return-void
    
    # 保留原始代码(已注释)
    # invoke-static {p1}, Landroid/text/TextUtils;->isEmpty(Ljava/lang/CharSequence;)Z
    # move-result v0
    # if-eqz v0, :cond_valid
    # 
    # const-string v0, "支付配置错误"
    # invoke-static {v0}, Lio/dcloud/common/util/ToastUtil;->show(Ljava/lang/String;)V
    # return-void
    # 
    # :cond_valid
    # invoke-direct {p0, p1, p2}, Lio/dcloud/feature/payment/PaymentFeatureImpl;->initPaymentSDK(Ljava/lang/String;Ljava/lang/String;)V
    # 
    # return-void
.end method
```

### 4. ActivityUtil类修改(捐赠者功能)

**类名:** `com.givemefive.ble.activity.ActivityUtil`
**Dalvik格式:** `Lcom/givemefive/ble/activity/ActivityUtil;`

#### 修改6: checkTk()方法

**方法签名:** `checkTk()Z`
**修改目标:** 直接返回true,解锁所有捐赠者功能

**原始代码:**
```smali
.method public static checkTk()Z
    .registers 5
    
    sget-object v0, Lcom/givemefive/ble/activity/ActivityUtil;->tk:Ljava/lang/String;
    invoke-static {v0}, Lorg/apache/commons/lang3/q;->A0(Ljava/lang/CharSequence;)Z
    move-result v0
    const/4 v1, 0x0
    if-eqz v0, :cond_0
    return v1
    
    :cond_0
    # ... 网络请求逻辑 ...
    
    if-nez v0, :cond_1
    const/4 v0, 0x1
    return v0
    
    :cond_1
    return v1
.end method
```

**修改后代码:**
```smali
.method public static checkTk()Z
    .registers 5
    
    # 直接返回true,跳过所有验证逻辑
    const/4 v0, 0x1
    return v0
    
    # 保留原始代码(已注释)
    # sget-object v0, Lcom/givemefive/ble/activity/ActivityUtil;->tk:Ljava/lang/String;
    # invoke-static {v0}, Lorg/apache/commons/lang3/q;->A0(Ljava/lang/CharSequence;)Z
    # move-result v0
    # const/4 v1, 0x0
    # if-eqz v0, :cond_0
    # return v1
    # 
    # :cond_0
    # ... 网络请求逻辑 ...
    # 
    # if-nez v0, :cond_1
    # const/4 v0, 0x1
    # return v0
    # 
    # :cond_1
    # return v1
.end method
```

## MT管理器操作步骤

### 步骤1: 打开APK

1. 在MT管理器中找到"表盘自定义工具_6.4.0.apk"
2. 长按APK文件 → 查看详情 → 打开方式 → APK查看
3. 进入classes.dex → Dex编辑器++

### 步骤2: 修改DCloudApplication类

1. Dex编辑器++ → 搜索 → 类名搜索
2. 输入: `io.dcloud.application.DCloudApplication`
3. 点击搜索结果进入类

#### 修改validateAppKey()方法

1. 在类中找到`validateAppKey()`方法
2. 长按方法 → 编辑
3. 在方法开头添加以下代码:
   ```smali
   const/4 v0, 0x1
   return v0
   ```
4. 删除或注释掉原有的验证逻辑
5. 保存修改

#### 修改validateAppKeyFromServer()方法

1. 在同一类中找到`validateAppKeyFromServer()`方法
2. 长按方法 → 编辑
3. 在方法开头添加以下代码:
   ```smali
   const/4 v0, 0x1
   return v0
   ```
4. 删除或注释掉原有的验证逻辑
5. 保存修改

#### 修改handleAppKeyError()方法

1. 在同一类中找到`handleAppKeyError()`方法
2. 长按方法 → 编辑
3. 清空方法内容,只保留:
   ```smali
   return-void
   ```
4. 保存修改

### 步骤3: 修改UniPushService类

1. Dex编辑器++ → 搜索 → 类名搜索
2. 输入: `io.dcloud.feature.unipush.UniPushService`
3. 点击搜索结果进入类
4. 找到`initialize(Ljava/lang/String;Ljava/lang/String;Ljava/lang/String;)V`方法
5. 长按方法 → 编辑
6. 清空方法内容,只保留:
   ```smali
   return-void
   ```
7. 保存修改

### 步骤4: 修改PaymentFeatureImpl类

1. Dex编辑器++ → 搜索 → 类名搜索
2. 输入: `io.dcloud.feature.payment.PaymentFeatureImpl`
3. 点击搜索结果进入类
4. 找到`initialize(Ljava/lang/String;Ljava/lang/String;)V`方法
5. 长按方法 → 编辑
6. 清空方法内容,只保留:
   ```smali
   return-void
   ```
7. 保存修改

### 步骤5: 修改ActivityUtil类(捐赠者功能)

1. Dex编辑器++ → 搜索 → 类名搜索
2. 输入: `com.givemefive.ble.activity.ActivityUtil`
3. 点击搜索结果进入类
4. 找到`checkTk()Z`方法
5. 长按方法 → 编辑
6. 在方法开头添加以下代码:
   ```smali
   const/4 v0, 0x1
   return v0
   ```
7. 删除或注释掉原有的验证逻辑
8. 保存修改

### 步骤6: 保存并签名

1. 保存所有修改 → 返回
2. MT管理器会自动重新打包并签名
3. 等待打包完成

### 步骤7: 重命名APK

1. 找到修改后的APK文件
2. 长按 → 重命名
3. 输入: `表盘自定义工具_6.4.0_终极破解版.apk`
4. 确认重命名

## 电脑端修改方案(apktool)

如果需要使用电脑端工具修改,可以使用apktool:

### 步骤1: 反编译APK

```bash
apktool d 表盘自定义工具_6.4.0.apk -o watchface_mod
```

### 步骤2: 修改Smali文件

找到以下文件并进行修改:

#### DCloudApplication.smali

**路径:** `watchface_mod/smali/io/dcloud/application/DCloudApplication.smali`

修改以下方法:
- `validateAppKey()Z` - 在方法开头添加 `const/4 v0, 0x1` 和 `return v0`
- `validateAppKeyFromServer()Z` - 在方法开头添加 `const/4 v0, 0x1` 和 `return v0`
- `handleAppKeyError()V` - 清空方法内容,只保留 `return-void`

#### UniPushService.smali

**路径:** `watchface_mod/smali/io/dcloud/feature/unipush/UniPushService.smali`

修改 `initialize(Ljava/lang/String;Ljava/lang/String;Ljava/lang/String;)V` 方法:
- 清空方法内容,只保留 `return-void`

#### PaymentFeatureImpl.smali

**路径:** `watchface_mod/smali/io/dcloud/feature/payment/PaymentFeatureImpl.smali`

修改 `initialize(Ljava/lang/String;Ljava/lang/String;)V` 方法:
- 清空方法内容,只保留 `return-void`

#### ActivityUtil.smali

**路径:** `watchface_mod/smali/com/givemefive/ble/activity/ActivityUtil.smali`

修改 `checkTk()Z` 方法:
- 在方法开头添加 `const/4 v0, 0x1` 和 `return v0`

### 步骤3: 重新打包

```bash
apktool b watchface_mod -o 表盘自定义工具_6.4.0_终极破解版_unsigned.apk
```

### 步骤4: 签名APK

```bash
# 使用apksigner签名(推荐)
apksigner sign --ks your_keystore.jks --ks-key-alias your_alias --ks-pass pass:your_password --key-pass pass:your_key_password --out 表盘自定义工具_6.4.0_终极破解版.apk 表盘自定义工具_6.4.0_终极破解版_unsigned.apk

# 或使用jarsigner签名(旧方法)
jarsigner -verbose -keystore your_keystore.jks -storepass your_password -keypass your_key_password -signedjar 表盘自定义工具_6.4.0_终极破解版.apk 表盘自定义工具_6.4.0_终极破解版_unsigned.apk your_alias
```

## 验证与测试

### 功能验证清单

| 功能 | 验证方法 | 预期结果 |
|------|----------|----------|
| 应用启动 | 启动应用 | 无配置错误提示,正常启动 |
| 蓝牙安装 | 打开BLEActivity,尝试安装表盘 | 无"未捐赠"提示,正常使用 |
| 智能安装 | 打开BLEActivityMi8Pro,尝试安装 | 无"未捐赠"提示,正常使用 |
| 图片替换 | 在设计界面尝试替换图片 | 无"未捐赠"提示,正常使用 |
| 预览功能 | 打开PreviewActivity,尝试预览 | 无"该功能仅限捐赠者"提示 |
| 推送服务 | 检查推送功能 | 推送服务已禁用,无推送通知 |
| 支付功能 | 检查支付功能 | 支付服务已禁用,无支付入口 |

### 稳定性测试

**测试项目:**
1. 应用启动是否正常
2. 所有功能是否可用
3. 是否出现崩溃或异常
4. 网络请求是否正常
5. 数据保存是否正常

**测试步骤:**
1. 安装修改后的APK
2. 启动应用,检查是否正常运行
3. 测试所有功能,记录异常
4. 长时间使用,观察稳定性
5. 重启应用,验证修改持久性

## 注意事项

### 重要提示

1. **签名校验:** 修改后的APK签名会改变,如果应用有签名校验机制,需要额外处理
2. **服务器验证:** 部分功能可能依赖服务器验证,本地修改可能无法完全绕过
3. **功能耦合:** 禁用推送和支付可能影响其他功能,需要测试验证
4. **数据安全:** 建议备份原始APK,避免数据丢失

### 风险评估

| 风险项 | 风险等级 | 说明 |
|--------|----------|------|
| 签名校验 | 中 | 应用可能有签名校验,需要额外处理 |
| 服务器检测 | 低 | 服务器可能检测异常行为 |
| 功能异常 | 低 | 修改可能影响其他功能,导致崩溃 |
| 数据异常 | 低 | 修改可能影响数据保存,导致数据丢失 |

### 应对措施

1. **签名校验:** 搜索签名验证代码并移除,或使用Lucky Patcher/核心破解
2. **服务器检测:** 使用测试账号,避免主账号被封
3. **功能异常:** 仔细测试所有功能,发现异常及时修复
4. **数据异常:** 备份重要数据,避免数据丢失

## 法律与道德声明

**重要提示:**
- 破解应用可能违反软件许可协议
- 可能侵犯开发者权益
- 仅用于学习和研究目的
- 不建议用于商业用途

**建议:**
- 支持正版,购买捐赠者资格
- 尊重开发者劳动成果
- 合理使用破解技术

## 总结

本次修改实现了以下目标:
- ✅ 绕过DCloud Appkey验证
- ✅ 禁用推送服务
- ✅ 禁用支付服务
- ✅ 解锁所有捐赠者功能

**修改难度:** 简单(★☆☆☆☆)
**稳定性:** 高(★★★★★)
**推荐工具:** MT管理器

---

*修改指南生成时间: 2026-06-23*
*基于: 表盘自定义工具_DCloud_Appkey验证机制分析报告.md*
*工具: MT管理器 MCP APK Analyzer*