# 🔍 APK 安全分析报告

## 📱 基本信息

| 项目 | 内容 |
|------|------|
| **应用名称** | 酷我音乐 |
| **包名** | cn.kuwo.player |
| **版本** | 12.1.4.0 (12140) |
| **文件名** | kwyy.apk |
| **类数量** | 118,191 个 |
| **DEX文件数** | 22 个 |
| **目标SDK** | 33 (Android 13) |
| **分析时间** | 2026-05-24 |

---

# 第一部分：原有功能分析

## ⚠️ 关键发现总结

### 🔴 1. 收费功能分析 - **存在且显著**

#### 发现的收费相关模块：

| 模块名称 | 功能描述 | 风险等级 |
|----------|----------|----------|
| `AdVipDialogFloatMgrImpl` | VIP广告对话框浮动管理器 | 🔴 高 |
| `AllPayConfig` | 全支付配置模块 | 🔴 高 |
| `AllPayIconView` | 全支付图标视图 | 🟡 中 |
| `AdVipIconView` | 广告VIP图标视图 | 🟡 中 |
| `KwRingVipBarView` | 酷我铃声VIP栏视图 | 🟡 中 |
| `VipUserInfo` | VIP用户信息管理 | 🟡 中 |
| `MusicPayInfo` | 音乐付费信息 | 🔴 高 |

#### 支付域名发现：

```
✅ musicpay.kuwo.cn        - 音乐支付服务
✅ audiobookpay.kuwo.cn    - 有声书支付服务
✅ vip1.kuwo.cn            - VIP服务
✅ alipaysdk               - 支付宝SDK集成
```

#### 收费提示文案：

- `"免费模式邀你畅听VIP歌曲"`
- `"尊贵的VIP，已为您跳过广告"`
- `"minibar->广告会员引导->取消"`
- `"播放器->广告会员引导->取消"`

---

### 🔴 2. 网络远程弹窗分析 - **存在且活跃**

#### 弹窗相关类：

| 类名 | 功能 | 位置 |
|------|------|------|
| `PopupInfo` | 弹窗信息配置类 | `cn.kuwo.mod.mobilead.freemode.bean` |
| `AdClosePopupInfo` | 广告关闭弹窗信息 | `cn.kuwo.mod.mobilead.freemode.bean` |
| `AdVipDialogFloatMgrImpl` | VIP广告弹窗管理器 | `ad.e` |

#### PopupInfo 类字段分析：

```java
public class PopupInfo {
    private int num;              // 弹窗数量
    private String openButton;    // 打开按钮文本
    private int showSecond;       // 显示时长(秒)
    private int songNumLimit;     // 歌曲数量限制
    private String subtitle;      // 副标题
    private String text;          // 弹窗文本
    private int timesLimit;       // 显示次数限制
    private String title;         // 标题
}
```

#### 弹窗控制键值：

```
key_ad_vip_web_date           - VIP网页广告日期
key_ad_vip_web_played_num     - VIP网页广告播放次数
key_ad_vip_web_showed_num     - VIP网页广告展示次数
key_ad_vip_minibar_tip_date   - 迷你栏提示日期
key_ad_vip_minibar_tip_played_num - 迷你栏播放次数
key_ad_vip_minibar_tip_showed_num - 迷你栏展示次数
```

#### WebView 远程加载能力：

| WebView类 | 用途 |
|-----------|------|
| `VipOpenWebView` | VIP相关网页打开 |
| `KwWebView` | 酷我自定义WebView |
| `PluginWebView` | 插件化WebView |
| `BridgeWebView` | JS桥接WebView (阿里安全) |
| `SafeWebView` | 安全WebView (华为) |

#### 远程配置URL：

```
https://h5app.kuwo.cn/m/audiomodeldialog/index.html
http://g.kuwo.cn/g.real?
https://fx.service.kugou.com/platform_rank/rank/weekStar/getConfig
```

---

### 🟡 3. 敏感权限分析

| 权限 | 风险 | 用途推测 |
|------|------|----------|
| `SYSTEM_ALERT_WINDOW` | 🔴 高 | 可显示悬浮窗/弹窗广告 |
| `INTERNET` | 🟡 中 | 网络访问 |
| `REQUEST_INSTALL_PACKAGES` | 🟡 中 | 可安装应用 |
| `ACCESS_FINE_LOCATION` | 🟡 中 | 精确定位 |
| `CAMERA` | 🟡 中 | 相机权限 |
| `RECORD_AUDIO` | 🟡 中 | 录音权限 |
| `READ_CALENDAR` | 🟡 中 | 读取日历 |
| `WRITE_CALENDAR` | 🟡 中 | 写入日历 |
| `usesCleartextTraffic=true` | 🟡 中 | 允许HTTP明文传输 |

---

### 🟡 4. 敏感字符串发现

| 类型 | 发现数量 | 示例 |
|------|----------|------|
| token | 30+ | `access_token`, `getToken()` |
| secretKey | 5+ | `secretKey is null` |
| password | 3+ | `userPassword` |

---

## 📊 原有功能风险评估

### 总体风险等级: 🔴 **中高风险**

| 风险类别 | 等级 | 说明 |
|----------|------|------|
| **隐藏收费** | 🔴 高 | 存在完整的VIP付费体系和广告引导机制 |
| **远程弹窗** | 🔴 高 | 具备远程配置弹窗能力，可控制显示频率和内容 |
| **数据收集** | 🟡 中 | 收集位置、设备信息等 |
| **隐私风险** | 🟡 中 | 多项敏感权限申请 |

---

# 第二部分：第三方修改分析

## 🚨 第三方修改分析 - 严重安全警告

### ⚠️ 总体评估: 🔴 **极高危险 - 已被第三方恶意修改**

---

## 🔴 一、第三方注入框架发现

### 1.1 注入框架标识

| 项目 | 详情 |
|------|------|
| **框架包名** | `BY.MT.YYDS` |
| **注入类数量** | 50+ 个类（A-Z, Aa-Zz等混淆命名） |
| **框架性质** | 完整的第三方验证/控制框架 |
| **注入方式** | DEX代码注入 + Native Hook |

### 1.2 注入框架核心组件

```
BY.MT.YYDS
├── vC          - 主控制类（验证、弹窗、Hook）
├── AD          - 点击事件Hook
├── ak          - 验证管理
├── sh          - 弹窗样式控制
├── Oi          - 网络请求/设备信息收集
├── Ms          - 流量劫持脚本
├── wr          - 拦截注入验证
└── ... (50+ 其他类)
```

---

## 🔴 二、Hook注入框架分析

### 2.1 Epic Hook框架

| 组件 | 功能 |
|------|------|
| `Epic.Jni.Hooker` | 主Hook框架类 |
| `Epic.Jni.Hooker$HookerFrame` | Hook框架接口 |
| `assets/Epic_so/liblsplant_%s.so` | LSPlant Native Hook库 |

### 2.2 AliuHook Xposed框架

| 组件 | 功能 |
|------|------|
| `aliucord.hook.XposedBridge` | Xposed Hook桥接 |
| `aliucord.hook.XC_MethodHook` | 方法Hook |
| `aliucord.hook.callbacks.XCallback` | 回调处理 |

### 2.3 Hook能力

```java
// 发现的Hook功能：
- ART_HOOK          // ART虚拟机Hook
- NATIVE_HOOK       // Native方法Hook
- Inline_Hooks      // 内联Hook
- hook_module       // 模块Hook
- hook_frame        // 框架Hook
- 注入Hooker Api    // API注入
```

### 2.4 Hook相关字符串

| 字符串 | 说明 |
|--------|------|
| `"Art Hook 模块缺失"` | ART Hook检测 |
| `"添加点击事件Hook规则:%s"` | 点击事件Hook |
| `"注入Hooker Api"` | API注入 |
| `"Hook到控件:%s"` | 控件Hook |
| `"开始Hook onClick:%s"` | onClick Hook |
| `"hook success! pid:"` | Hook成功 |

---

## 🔴 三、卡密验证系统

### 3.1 验证相关字符串

| 字符串 | 用途 |
|--------|------|
| `"卡密验证成功"` | 验证成功提示 |
| `"云端连接失败"` | 云端验证失败 |
| `"云端验证失败"` | 验证失败提示 |
| `"云端验证通过"` | 验证通过提示 |
| `"正在验证密码中...."` | 密码验证中 |
| `"拦截成功 开始注入验证"` | 注入验证触发 |
| `"有网，本地有上次保存的云验证卡密信息和token"` | 本地缓存验证 |
| `"清除本地保存的卡密信息"` | 清除缓存 |

### 3.2 卡密信息格式

```
卡密:%s
类型:%s
面值:%d
机器码:%s
备注:%s
激活次数:%d
首次激活时间:%s
到期时间:%s
创建时间:%s
卡密状态:%s
```

---

## 🔴 四、第三方远程控制服务器

### 4.1 可疑服务器地址

| 地址 | 端口 | 用途 | 风险等级 |
|------|------|------|----------|
| **111.173.115.85** | 81 | 远程验证/弹窗控制 | 🔴 极高 |
| 127.0.0.1 | 3456 | 本地代理服务 | 🟡 中 |

### 4.2 远程控制URL

```
http://111.173.115.85:81/shy/1.html   - 远程HTML页面加载
http://%s:%d/splash                   - 动态Splash广告
```

### 4.3 流量劫持代码

```groovy
// 发现的流量劫持脚本 (Groovy语法)
import Epic.Svc.Network.PackerHook
import Epic.Svc.Network.Result
import Epic.Svc.SvcSoLoader

// 构造HTTP响应数据
static def buildHttpResponse(int statusCode, String reasonPhrase, 
                             Map<String, String> headers, String body) {
    // 构建状态行...
}

// 设置需要重定向IP的端口
PackerHook.addRelocateIp("111.173.115.85", 81)  // 端口填写-1为所有端口

// 启动流量转发Hook 并设置代理为本地服务器地址和端口
PackerHook.hookConnect("127.0.0.1", 3456)

// 设置流量转发回调
PackerHook.addCallBack(...)
```

---

## 🔴 五、远程弹窗控制功能

### 5.1 弹窗控制字段

| 字段 | 说明 |
|------|------|
| `dialog_display_type` | 弹窗显示类型 |
| `dialog_title` | 弹窗标题 |
| `dialog_msg` | 弹窗消息 |
| `dialog_style` | 弹窗样式 |
| `Dialog模式:%s` | 弹窗模式日志 |

### 5.2 弹窗控制能力

```java
// 弹窗控制功能：
- AlertDialog 创建和显示
- 远程配置弹窗内容
- 控制弹窗显示时机 (start_time, end_time)
- 多种弹窗样式支持
- 展示时机控制
```

### 5.3 验证弹窗相关

| 字符串 | 说明 |
|--------|------|
| `"开始展示验证"` | 验证弹窗展示 |
| `"上次启动时间戳: "` | 时间戳记录 |
| `"展示时机: "` | 展示时机控制 |

---

## 🔴 六、推送服务分析

### 6.1 推送SDK

| SDK | 说明 |
|-----|------|
| `com.tme.push.matrix` | TME推送矩阵 |
| `com.cmic.promopush` | CMIC推广推送 (v2.2.0) |
| `cn.kuwo.mod.push` | 酷我自有推送 |
| `FAPushRuntimeImpl` | 繁星推送实现 |

### 6.2 推送服务器

```
androidpushserver.kuwo.cn  - 酷我推送服务器
```

### 6.3 推送相关功能

| 功能 | 说明 |
|------|------|
| `PushService` | 推送服务 |
| `PushActivity` | 推送活动页 |
| `PushWindowActivity` | 推送窗口活动 |
| `KwPushReceiver` | 酷我推送接收器 |

---

## 📊 七、安全风险评估

### 总体风险: 🔴 **极高危险**

| 风险类别 | 等级 | 详情 |
|----------|------|------|
| **第三方代码注入** | 🔴 极高 | BY.MT.YYDS框架完整注入 |
| **Hook框架植入** | 🔴 极高 | Epic + AliuHook双重Hook |
| **远程服务器通信** | 🔴 极高 | 111.173.115.85:81 |
| **流量劫持能力** | 🔴 极高 | 可重定向所有网络流量 |
| **卡密验证系统** | 🔴 高 | 完整的付费验证系统 |
| **远程弹窗控制** | 🔴 高 | 可远程控制弹窗显示 |
| **设备信息收集** | 🟡 中高 | 收集设备MAC、IP等 |
| **推送服务** | 🟡 中 | 多个推送SDK |

---

## 🎯 八、结论

### ✅ 确认存在第三方修改

该APK已被第三方恶意修改，植入了完整的控制框架：

#### 1. **注入框架** - BY.MT.YYDS
- 50+个注入类
- 完整的验证和控制功能
- 与第三方服务器通信

#### 2. **Hook框架** - Epic + AliuHook
- Native Hook (LSPlant)
- Java Hook (Xposed)
- 可Hook任意方法

#### 3. **卡密验证系统**
- 云端验证
- 本地缓存验证
- 到期时间控制

#### 4. **远程控制能力**
- 远程弹窗控制
- 远程HTML页面加载
- 流量劫持和重定向

#### 5. **第三方服务器**
- **111.173.115.85:81** - 可疑的第三方服务器
- 可能用于：
  - 卡密验证
  - 弹窗内容下发
  - 远程指令控制
  - 流量劫持

---

## ⚠️ 九、安全建议

### 🔴 强烈建议：

1. **不要安装此APK** - 存在极高的安全风险
2. **如已安装** - 立即卸载并清除数据
3. **检查网络** - 是否有与 111.173.115.85 的通信
4. **修改密码** - 如使用过相关账号，立即修改密码
5. **使用官方版本** - 从官方渠道下载正版应用

### 📋 此APK可能的功能：

- ✅ 远程控制弹窗显示
- ✅ 远程下发收费提醒
- ✅ 劫持所有网络流量
- ✅ 收集设备信息上传
- ✅ 卡密验证（可能是付费破解版）
- ✅ Hook应用核心功能

---

## 📋 十、技术细节汇总

### 10.1 注入类列表（部分）

| 类名 | 功能推测 |
|------|----------|
| `BY.MT.YYDS.vC` | 主控制器 |
| `BY.MT.YYDS.AD` | 点击Hook |
| `BY.MT.YYDS.ak` | 验证管理 |
| `BY.MT.YYDS.sh` | 弹窗样式 |
| `BY.MT.YYDS.Oi` | 网络请求 |
| `BY.MT.YYDS.Ms` | 流量劫持 |
| `BY.MT.YYDS.wr` | 拦截验证 |
| `BY.MT.YYDS.BH` | Hook模块 |
| `BY.MT.YYDS.Jn` | 卡密信息 |
| `BY.MT.YYDS.Ae` | 服务器配置 |

### 10.2 Hook框架文件

| 文件路径 | 说明 |
|----------|------|
| `assets/Epic_so/liblsplant_arm64-v8a.so` | ARM64 Hook库 |
| `assets/Epic_so/liblsplant_armeabi-v7a.so` | ARM32 Hook库 |
| `assets/Epic_so/liblsplant_x86.so` | x86 Hook库 |
| `assets/Epic_so/liblsplant_x86_64.so` | x64 Hook库 |

### 10.3 设备信息收集

根据代码分析，注入框架可能收集以下信息：

| 信息类型 | 说明 |
|----------|------|
| MAC地址 | 设备网络标识 |
| IP地址 | 网络位置信息 |
| 设备型号 | 手机型号 |
| Android版本 | 系统版本 |
| 应用列表 | 已安装应用 |
| 机器码 | 设备唯一标识 |

---

## 📊 风险等级说明

| 等级 | 说明 |
|------|------|
| 🔴 极高 | 可能导致严重安全问题的风险 |
| 🔴 高 | 存在明显安全隐患 |
| 🟡 中高 | 需要关注的安全问题 |
| 🟡 中 | 一般性安全问题 |
| 🟢 低 | 风险较低 |

---

**报告生成时间**: 2026-05-24  
**分析工具**: MT MCP APK 分析服务  
**最终风险等级**: 🔴 **极高危险**  
**建议操作**: 立即删除，切勿使用
